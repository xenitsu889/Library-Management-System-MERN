# Production backend environment
# On Render, leave PORT unset if the platform injects it automatically.

NODE_ENV=production
MONGO_URL=mongodb+srv://<db_username>:<db_password>@<cluster-host>/library-management-2?retryWrites=true&w=majority
